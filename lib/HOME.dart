import 'package:flutter/material.dart';
class MYPAGE extends StatelessWidget {
  const MYPAGE({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Cpnnection Successful'),
      ),
      body: Text('HEllo PInki'),
    );
  }
}